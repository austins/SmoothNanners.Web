@model Blazicon

<svg @Html.Raw(string.Join(' ', Model.Icon.Attributes.Select(kvp => $"{kvp.Key}=\"{kvp.Value}\"")))
     class="w-[1em] h-[1em] align-middle">
    @Html.Raw(Model.Icon.Content)
</svg>
