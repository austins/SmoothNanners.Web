@model SvgIcon
@{
    var iconAttributes = Model.Attributes.ToDictionary();
    iconAttributes["width"] = "1em";
    iconAttributes["height"] = "1em";
}

<svg @Html.Raw(string.Join(' ', iconAttributes.Select(x => $"{x.Key}=\"{x.Value}\"")))>
    @Html.Raw(Model.Content)
</svg>
