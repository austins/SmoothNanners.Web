@model IList<SmoothNanners.Web.Models.Portal.PortalLink>

@foreach (var link in Model)
{
    <a
        href="@link.Url"
        target="_blank"
        rel="noreferrer"
        class="btn btn-lg btn-@link.BackgroundVariant.ToStringFast() d-flex align-items-center">
        <svg bootstrap-icon="@link.Icon" class="me-2"></svg>
        <span>@link.Text</span>
    </a>
}
